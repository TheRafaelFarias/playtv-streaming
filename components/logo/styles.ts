import styled from "styled-components/native";
import { Text } from "../baseComponents";

export const PlayTVLogoText = styled(Text).attrs({
  size: 20,
  weight: "semibold",
})`
  margin-left: 7px;
`;
